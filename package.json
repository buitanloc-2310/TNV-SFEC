{
  "name": "sfn-tnv-portal",
  "private": true,
  "version": "1.0.0",
  "description": "Cổng Tình nguyện viên - Mạng lưới Giáo dục & Phát triển Cộng đồng Sky First (SFN)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check public/app.js",
    "db:schema:local": "wrangler d1 execute tnv-sfec --local --file=./schema.sql",
    "db:inspect:remote": "wrangler d1 execute tnv-sfec --remote --command=\"SELECT name FROM sqlite_schema WHERE type='table' ORDER BY name;\""
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
